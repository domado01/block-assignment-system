# CLAUDE.md — 프로젝트 가이드

> 이 문서는 다른 AI 에이전트 또는 개발자가 본 프로젝트를 이해하고 유지보수할 수 있도록 작성된 안내서입니다. **작업 시작 전 반드시 이 문서를 먼저 읽으세요.**

---

## 1. 프로젝트 개요

블록 단위 생산 계획을 다루는 데모용 **작업장 조업도 / 블록 배정 시스템**입니다.

- Python으로 합성 데이터(블록 1만 행, 작업장 15행)를 생성
- 8단계 로직으로 각 블록의 `최종작업장`을 결정
- React + Vite 대시보드에서 월별 조업도, 배정 결과, 배정 로직 다이어그램을 시각화

핵심 가치:
- **결정론적 데이터** — `random.seed(42)` 고정. 동일 코드 → 동일 결과
- **단일 source of truth (Python)** — 모든 로직과 결정은 `generate_excel.py` 에 있음. 웹은 그 결과를 표시만 함
- **읽기 전용 대시보드** — 로직을 다이어그램·표로 이해할 수 있고, 단계별 처리 건수·초과 테이블 등을 확인 가능

---

## 2. 빠른 시작

```bash
# 1) 데이터 생성 (엑셀 2개 + JSON 2개)
python -X utf8 generate_excel.py

# 2) 웹앱 의존성 설치 (최초 1회)
cd web
npm install

# 3) 개발 서버
npm run dev          # → http://localhost:5173/

# 4) 프로덕션 빌드 (검증)
npm run build
```

**요구사항**: Python 3.10+, Node 18+, `pandas`, `openpyxl`

---

## 3. 저장소 구조

```
.
├── CLAUDE.md                   ← 이 문서 (AI 인수인계용)
├── README.md                   ← 사람용 짧은 안내
├── .gitignore
├── generate_excel.py           ← 데이터 + 배정 (Python, source of truth)
├── calc_op_rate.py             ← area3~5 조업도 산출 유틸
├── 1번_블록테이블.xlsx          ← 생성된 블록 데이터 (10,000행 × 13열)
├── 2번_작업장테이블.xlsx        ← 생성된 작업장 데이터 (15행 × 38열)
└── web/                        ← React + Vite 대시보드
    ├── package.json
    ├── vite.config.js
    ├── index.html
    └── src/
        ├── main.jsx
        ├── App.jsx              ← 탭 라우팅 + 단순 상태
        ├── styles.css           ← 전체 스타일
        ├── util.js              ← 포맷·색상 헬퍼
        ├── data/
        │   ├── data.json        ← 집계 + 단계별 결과 + 초과표
        │   └── blocks.json      ← 블록 레벨 데이터 (~1 MB, 현재 미사용)
        ├── lib/
        │   └── assignment.js    ← 빈 스텁 (구버전 재배정 로직 제거)
        └── components/
            ├── OperationHeatmap.jsx
            ├── MonthlyChart.jsx
            ├── AssignmentView.jsx
            └── LogicView.jsx    ← 다이어그램 + 단계별 결과 + 초과 표
```

---

## 4. 데이터 모델

### 4.1 1번 블록 테이블 — `1번_블록테이블.xlsx` (10,000행 × 16열)

| 컬럼 | 설명 |
|---|---|
| `블록명` | `m000a`~`m999z` 형식 고유 ID. 중복 없음 |
| `물성` | `대` / `중` / `소` (대략 1/3씩) |
| `기준계획작업장` | `area1`~`area15` 랜덤 |
| `H/T` | `H` 또는 `T` |
| `JG` | `L` / `D` / `F` 랜덤 (분류 코드, 약 1/3씩) |
| `PC` | `P` / `C` 랜덤 (분류 코드, 약 1/2씩) |
| `PRJ` | `A` / `B` / `C` / `D` 랜덤 (프로젝트 코드, 약 1/4씩) |
| `공수` | 실수, 약 0.43~108.70 (원본 정수 10~2500에 1/23 적용, 소수 2자리 반올림) |
| `착수일` | 2026-01-01 ~ 2026-02-28 사이 랜덤 |
| `부모블록` | 물성이 `소`인 행만 `area1`~`area15` 랜덤, 그 외 공란 |
| `선호작업장1` | `area1`~`area15` 또는 `동일` (약 20% 확률) |
| `선호작업장2~5` | `area1`~`area15` (한 행 내 5개 모두 서로 다름) |
| `최종작업장` | 8단계 로직 결과. `area1~15` / `미지정` / 공란 셋 중 하나 |

### 4.2 2번 작업장 테이블 — `2번_작업장테이블.xlsx` (15행 × 38열)

| 컬럼 | 설명 |
|---|---|
| `작업장` | `area1`~`area15` |
| `우선순위` | 1~15 중복 없는 순열 |
| `1월능력`~`12월능력` | 정수 500~6000 |
| `1월부하`~`12월부하` | 정수 500~6000 |
| `1월평균조업도`~`12월평균조업도` | 월별 고정값(전 작업장 동일). 1월 90%, 2월 110%, ..., 12월 85% |

⚠ **주의**: `평균조업도` 컬럼은 사전 정의된 고정값입니다. 8단계 로직이 사용하는 **목표 조업도**(= Σ부하/Σ능력)와는 다른 값입니다.

### 4.3 `web/src/data/data.json`

```jsonc
{
  "generatedAt": "YYYY-MM-DD HH:MM",
  "months": ["1월", ..., "12월"],
  "blockCount": 10000,
  "unassignedCount": 4734,                // 공란 (T+소비동일, H+대중)
  "workshops": [ { name, priority, capacity[12], load[12], operationRate[12] } ],
  "assignment": [
    { "workshop": "area1", "blockCount": ..., "totalManhours": ... },
    ...
    { "workshop": "미지정", "blockCount": ..., "totalManhours": ... }
  ],
  "targetRate": [/* 12개 fraction, 목표 조업도 */],
  "targetManhours": { "area1": [/* 12개 */], ... },
  "overloadTable": [
    {
      "month": 1,
      "h_so_manhours": ...,
      "remaining_capacity": ...,
      "threshold_120pct": ...,
      "excess": ...,
      "is_over": true|false
    }
  ],
  "stepCounts": { "step2": ..., "step3": ..., "step7": ..., "step8": ... }
}
```

### 4.4 `web/src/data/blocks.json`

블록 레벨 데이터 1만 행 배열 (현재 웹에서 임포트하지 않음). 향후 브라우저 재계산 기능을 추가할 때 사용 가능.

---

## 5. 최종작업장 배정 로직 — 8단계

**모든 로직은 `generate_excel.py` 단독으로 결정**됩니다 (웹에는 결과만 표시).

### 단계 1 — 목표 조업도·공수 산출
- `목표 조업도[월] = Σ_(모든 작업장) 부하[작업장,월] / Σ 능력[작업장,월]`
- `목표 공수[작업장,월] = 목표 조업도[월] × 능력[작업장,월]`

### 단계 2 — T + 대/중 → 기준계획작업장
- 조건: `H/T == 'T'` AND `물성 ∈ {대, 중}`
- 액션: `최종작업장 ← 기준계획작업장`
- 능력 초과 무시

### 단계 3 — T + 소 + 동일 → 부모블록
- 조건: 단계 2 미해당 AND `H/T == 'T'` AND `물성 == '소'` AND `선호작업장1 == '동일'`
- 액션: `최종작업장 ← 부모블록`

### 단계 4 — H+소 공수의 월별 합산
- `H/T == 'H'` AND `물성 == '소'` 블록의 공수를 **착수일 월** 기준으로 합산 (분석용)

### 단계 5 — area3~5 월별 잔여 능력
- `잔여능력[월] = (area3 능력[월] + area4 능력[월] + area5 능력[월]) − (최종작업장이 area3~5인 블록의 공수 월별 합)`
- 단계 2·3에서 area3~5에 들어간 공수가 능력보다 많으면 **음수가 될 수 있음** (의도된 동작)

### 단계 6 — 초과 검사
- 단계 4의 월별 H+소 공수가 `잔여능력 × 120%` 를 초과하는 월을 식별
- 결과: `overloadTable` (월별 표) — 웹앱 로직 관리 탭에 노출

### 단계 7 — H+소 미배정 → area3·4·5 라운드로빈
- 조건: 단계 2·3에서 미배정 AND `H/T == 'H'` AND `물성 == '소'`
- 처리:
  1. 대상 블록을 `착수일` 오름차순으로 정렬
  2. 시작 위치 `area3` → 블록을 그 area에 시험 배정
  3. 해당 area의 월별 누적공수가 `목표 공수[area][월]` 를 초과하면 다음 area 시도 (3 → 4 → 5)
  4. `area5` 도 초과하면 그 블록은 **배정하지 않음** (이후 단계 8에서 '미지정' 처리)
  5. 다음 블록의 시작 위치는 **이전 블록이 들어간 area의 다음**:
     - 이전 area3 → 다음 시작 area4
     - 이전 area4 → 다음 시작 area5
     - 이전 area5 → 다음 시작 area3 (순환)
     - 이전 블록이 미배정 → area3으로 리셋

### 단계 8 — H+소 잔여 → '미지정'
- 위 단계들로도 미배정인 `H+소` 블록의 `최종작업장`에 텍스트 `'미지정'` 입력

### 공란(빈 셀)로 남는 케이스
- `H/T == 'T'` + `물성 == '소'` + `선호1 ≠ '동일'`
- `H/T == 'H'` + `물성 ∈ {대, 중}`

위 두 케이스는 어떤 단계도 처리하지 않으므로 `최종작업장` 이 공란으로 남습니다.

### 결과 분포 (현재 seed=42 기준)

| 항목 | 건수 |
|---|---|
| 단계 2 (T+대중 → 기준계획작업장) | 3,320 |
| 단계 3 (T+소+동일 → 부모블록) | 315 |
| 단계 7 (H+소 → area3~5 RR) | 0 ※ |
| 단계 8 (미지정) | 1,631 |
| 공란 | 4,734 |
| **합계** | **10,000** |

※ 잔여능력이 1·2월에 음수(이미지정 공수 >> area3~5 능력)인 데이터 특성상 목표공수 한도가 음수가 되어 단계 7이 0건 배정. 의도된 동작이며 단계 6의 초과 테이블이 이 상태를 보여줍니다.

---

## 6. 웹앱 아키텍처

### 6.1 상태 관리 (App.jsx)

| 상태 | 의미 |
|---|---|
| `tab` | 활성 탭: `'operation'` / `'assignment'` / `'logic'` |
| `metric` | 히트맵 metric: `'rate'` / `'capacity'` / `'load'` |
| `selected` | 상세 차트 작업장명 |

⚠ 이전 버전에 있던 로직 편집·재배정·localStorage 저장 기능은 8단계 로직 전면 교체와 함께 **제거**되었습니다. 웹앱은 이제 순수 표시 전용입니다.

### 6.2 데이터 흐름

```
generate_excel.py ─→ data.json ─→ App.jsx (정적 import)
                                       │
                                       ├─→ OperationHeatmap   (월간 조업도 탭)
                                       ├─→ MonthlyChart       (작업장 상세 차트)
                                       ├─→ AssignmentView     (배정 현황 탭)
                                       └─→ LogicView          (로직 관리 탭)
```

### 6.3 주요 컴포넌트

- **`OperationHeatmap.jsx`** — 작업장 × 12개월 히트맵. 조업도/능력/부하 토글. 클릭 시 상세 표시.
- **`MonthlyChart.jsx`** — 의존성 없는 순수 SVG 막대 차트.
- **`AssignmentView.jsx`** — 작업장별 배정 카드 + 표. `'미지정'` 행은 시각적으로 구분(주황 배경 + 이탤릭).
- **`LogicView.jsx`** — 8단계 다이어그램 + 단계별 처리 건수 카드 + 단계 1 월별 목표 조업도 표 + 단계 6 초과 테이블.

### 6.4 스타일

- 단일 파일 `src/styles.css`
- CSS 변수: `--accent` (#2563eb), `--bg` (#f1f5f9), `--panel` (#fff)
- 외부 UI 라이브러리 없음

---

## 7. 자주 하는 변경 (Cookbook)

### 7.1 1번 테이블에 새 컬럼 추가
1. `generate_excel.py`의 `rows1` 딕셔너리에 키 추가
2. 웹앱이 새 필드를 사용해야 하면 `blocks_data` 항목과 (필요 시) JSON export, 그리고 해당 컴포넌트 수정
3. `python -X utf8 generate_excel.py` 재실행

### 7.2 배정 로직 변경
1. `generate_excel.py`의 `최종작업장 배정 — 8단계 로직` 섹션을 수정
2. JSON output 구조가 바뀌면 `web/src/components/LogicView.jsx`, `AssignmentView.jsx`, `App.jsx` 도 갱신
3. `python -X utf8 generate_excel.py` 실행
4. `cd web && npm run build` 로 검증

### 7.3 새 탭 추가
1. `App.jsx`의 `<nav className="tabs">`에 버튼 추가
2. `{tab === '새탭이름' && ...}` 조건부 렌더링 추가
3. 컴포넌트는 `src/components/`에 신규 파일

### 7.4 데이터 갱신
`python -X utf8 generate_excel.py`만 실행. Vite HMR가 JSON 변경을 감지해 화면을 자동 갱신.

---

## 8. 알려진 제약 / 주의사항

### 8.1 엑셀 파일 잠금
`1번_블록테이블.xlsx`를 Excel에서 열어둔 채 스크립트 실행 시 `PermissionError`. 스크립트가 `try/except`로 잡아 JSON은 정상 생성되지만 엑셀은 갱신되지 않습니다. 갱신이 필요하면 사용자가 파일을 먼저 닫아야 합니다.

### 8.2 PowerShell 한글 인코딩
`python generate_excel.py`를 그대로 실행하면 stdout이 cp949로 깨질 수 있음. **반드시 `python -X utf8`** 옵션을 붙여 실행하세요.

### 8.3 평균조업도 ≠ 목표조업도
2번 테이블의 `평균조업도` 컬럼은 월별 고정값(전 작업장 동일). 8단계 로직의 단계 1에서 산출하는 **목표조업도**(Σ부하/Σ능력)와는 다른 값입니다. 화면의 "월간 조업도" 탭은 평균조업도 컬럼을 표시, 로직 탭은 목표조업도를 표시합니다.

### 8.4 데이터 스케일 — 단계 5 음수 잔여능력
2번 테이블 월별 능력이 500~6000인데 블록 평균 공수가 ~1,255라, 단계 2에서 area3~5에 들어가는 블록 수십 개만으로도 능력을 압도적으로 넘깁니다. 따라서 단계 5의 잔여능력이 음수가 되고, 단계 6은 ★초과, 단계 7은 0건 배정으로 귀결됩니다. **데이터 특성**이지 로직 결함이 아닙니다.

### 8.5 결정론
모든 랜덤은 `random.seed(42)` 고정. 코드/시드가 같으면 동일 데이터·동일 결과가 생성됩니다.

### 8.6 웹앱은 표시 전용
이제 웹에서는 로직을 편집·저장할 수 없습니다. 로직 변경은 `generate_excel.py` 수정 → 재실행 → 페이지 새로고침으로 일관되게 적용됩니다.

---

## 9. 빌드 / 배포

- **개발**: `npm run dev` (Vite HMR)
- **빌드**: `npm run build` → `web/dist/` (정적 파일, gzip 후 ~52 KB)
- **미리보기**: `npm run preview`

백엔드 없음. 정적 파일 서버 어디든 배포 가능 (Netlify / GitHub Pages / Vercel 등).

---

## 10. 작업 이력

- v1 — 초기 데이터 합성 + Excel 출력
- v2 — React + Vite 대시보드 (월간 조업도 + 배정 현황 2탭)
- v3 — 최종작업장 배정 로직 (단계 0~3)
- v4 — 착수일 컬럼 + 라운드로빈 단계 3 + 1월 능력 한도
- v5 — 로직 관리 탭 (다이어그램 + 편집 + 저장 + 재배정)
- **v6** — 배정 로직 전면 교체 (목표 조업도/공수 기반 8단계). 웹의 로직 편집 기능 제거 (표시 전용)
- **v6.1** — 1번 테이블 `공수` 컬럼에 1/23 적용 (능력 대비 균형 조정). 단계 2~3에서 area3~5에 누적되는 공수가 ~23배 줄어들지만 능력 대비로는 여전히 큼 → 단계 7은 여전히 0건 배정(시드 42 기준)
- **v6.2** — 1번 테이블에 분류 코드 컬럼 추가: `JG` (L/D/F), `PC` (P/C), `PRJ` (A/B/C/D). 총 13 → 16 컬럼. 현재는 배정 로직과 무관하나 향후 확장용 메타데이터

### 향후 가능 작업
- 단계 7 활성화를 위한 데이터 스케일 조정 옵션 (능력·공수 비율)
- 단계 6 초과 시 자동 알림 / 보고서
- 브라우저에서 즉시 시뮬레이션 (assignment.js를 8단계 로직으로 부활)
- `평균조업도` 컬럼을 `목표조업도` 와 일치시키는 옵션
