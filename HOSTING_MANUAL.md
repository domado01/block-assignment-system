# 웹앱 호스팅 매뉴얼 (초보자용)

이 문서는 **컴퓨터 명령어를 잘 모르는 분**이 다른 PC에 웹앱을 띄울 수 있도록 작성된 상세 안내서입니다.

- 예상 소요 시간: **약 15분** (처음 한 번만, 다음부터는 1~2분)
- 필요한 것: Windows 10/11 PC, 인터넷 연결
- ⚠ 컴퓨터에 **소프트웨어 1개를 설치**해야 합니다 (Node.js)
- Python은 설치 안 해도 됩니다

---

## 목차

1. [준비물 확인](#1-준비물-확인)
2. [Node.js 설치](#2-nodejs-설치)
3. [코드 다운로드](#3-코드-다운로드)
4. [의존성 설치 (npm install)](#4-의존성-설치-npm-install)
5. [웹앱 실행](#5-웹앱-실행)
6. [같은 네트워크의 다른 사람도 접속하게 만들기](#6-같은-네트워크의-다른-사람도-접속하게-만들기)
7. [방화벽 허용](#7-방화벽-허용-필요할-때만)
8. [종료 및 재시작](#8-종료-및-재시작)
9. [문제 해결 모음](#9-문제-해결-모음)
10. [용어 사전](#10-용어-사전)

---

## 1. 준비물 확인

| 항목 | 필요 |
|---|---|
| Windows 10 또는 11 PC | ✅ |
| 인터넷 연결 (설치할 때만 필요) | ✅ |
| 관리자 권한 (Node.js 설치 시 묻습니다) | ✅ |
| GitHub 계정 | ❌ (없어도 OK) |
| Python | ❌ (불필요) |
| Visual Studio / 개발 도구 | ❌ (불필요) |

---

## 2. Node.js 설치

Node.js는 이 웹앱을 실행시켜주는 **엔진** 같은 프로그램입니다. 한 번만 설치하면 됩니다.

### 단계 2-1: 다운로드

1. 브라우저(크롬·엣지·웨일 등 아무거나)를 엽니다.
2. 주소창에 입력 → 엔터:
   ```
   https://nodejs.org/ko/
   ```
3. 화면 가운데에 큰 초록색 버튼 두 개가 보입니다.
   - **LTS** 버튼 (왼쪽, "권장") ← 이걸 클릭
   - "현재"는 누르지 마세요
4. 다운로드 받은 `node-vXX.XX.X-x64.msi` 파일을 더블클릭

### 단계 2-2: 설치

설치 마법사가 뜹니다. 화면대로 따라하세요:

1. **Welcome** 화면 → **"Next"** 클릭
2. **License Agreement** → "I accept..." 체크 → **"Next"**
3. **Destination Folder** → 그대로 두고 **"Next"**
4. **Custom Setup** → 그대로 두고 **"Next"**
5. **Tools for Native Modules** 화면이 나오면 → ⚠ **체크하지 마세요** → **"Next"**
   (체크하면 추가로 큰 파일 더 받아야 함)
6. **Ready to install** → **"Install"** 클릭
7. 사용자 계정 컨트롤(UAC) 창이 뜨면 **"예"**
8. 설치 완료 → **"Finish"**

### 단계 2-3: 설치 확인

설치가 잘 됐는지 확인합니다.

1. **Windows 키 + R** 동시에 누르기 (실행창 뜸)
2. 작은 창에 `powershell` 입력 → **확인**
3. 파란색 글자가 나오는 검은 창이 뜸 (이게 **PowerShell**)
4. 다음 명령어 입력 → 엔터:
   ```
   node --version
   ```
5. `v20.XX.X` 같은 글자가 나오면 ✅ 성공
   - 나오지 않거나 빨간 글씨면 PC를 한 번 재시작 후 다시 시도

---

## 3. 코드 다운로드

웹앱 코드를 PC에 받습니다. **Git 같은 별도 프로그램 없이** 가능합니다.

### 단계 3-1: GitHub에서 ZIP 받기

1. 브라우저에서 다음 주소 열기:
   ```
   https://github.com/domado01/block-assignment-system
   ```
   ⚠ 페이지가 "404 Not Found" 또는 로그인 요구하면 → **저장소가 아직 Private** 상태일 수 있습니다. 저장소 주인에게 Public으로 바꿔 달라고 요청하거나, GitHub 계정을 초대받아야 합니다.

2. 화면 중간 즈음에 **초록색 "<> Code"** 버튼이 보입니다 → 클릭
3. 드롭다운 맨 아래 **"Download ZIP"** 클릭
4. `block-assignment-system-main.zip` 파일이 다운로드 폴더로 받아짐 (보통 `C:\Users\본인이름\Downloads`)

### 단계 3-2: 압축 풀기

1. 다운로드 받은 ZIP 파일을 **오른쪽 마우스 클릭**
2. **"압축 풀기..."** 또는 **"여기에 풀기"** 선택
3. 원하는 위치에 풀기. 예시:
   - `C:\projects\block-assignment-system-main`
   - 또는 바탕화면도 OK
4. 풀고 나면 폴더 안에 다음 같은 파일들이 보여야 함:
   - `web/` 폴더
   - `generate_excel.py`
   - `CLAUDE.md`
   - `README.md`
   - `HOSTING_MANUAL.md` (이 문서)
   - 등등

### 단계 3-3: 폴더 경로 외워두기

이 폴더의 **전체 경로**를 알아두세요. 탐색기에서 위쪽 주소창 클릭하면 보입니다.
예: `C:\Users\본인이름\Downloads\block-assignment-system-main`

이후 PowerShell에서 자주 사용합니다.

---

## 4. 의존성 설치 (npm install)

웹앱이 사용하는 라이브러리들을 한 번에 받아오는 단계입니다.

### 단계 4-1: PowerShell 열기

방법 1 (간단):
1. 압축 푼 폴더 안으로 들어가서 → `web` 폴더로 들어감
2. 폴더 안 빈 공간에서 **Shift + 오른쪽 마우스 클릭**
3. **"여기에 PowerShell 창 열기"** 선택
   - 안 보이면 "여기서 명령 창 열기"

방법 2 (수동):
1. **Windows 키 + R** → `powershell` 입력 → 확인
2. 검은 창에서 폴더로 이동:
   ```
   cd "C:\Users\본인이름\Downloads\block-assignment-system-main\web"
   ```
   (경로는 본인 환경에 맞게)

### 단계 4-2: 설치 명령 실행

PowerShell 창에 다음 입력 → 엔터:

```
npm install
```

- 30초 ~ 2분 정도 걸립니다 (네트워크 속도에 따라)
- 노란색·빨간색 경고 메시지가 나와도 **무시 OK**
- 마지막에 `added XX packages...` 같은 문장이 나오면 ✅ 성공
- 다 끝나면 폴더에 `node_modules` 라는 큰 폴더가 생김 (300MB 정도)

### 단계 4-3: 확인

설치 성공 여부 확인:
```
dir
```
출력 목록에 `node_modules`, `package.json`, `src` 등이 보이면 OK.

---

## 5. 웹앱 실행

이제 웹앱을 켤 수 있습니다.

### 단계 5-1: 실행 명령

같은 PowerShell 창에서 입력:

```
npm run dev
```

다음과 비슷한 메시지가 나옵니다:
```
  VITE v5.4.21  ready in 540 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 단계 5-2: 브라우저에서 보기

1. 브라우저(크롬·엣지) 열기
2. 주소창에 입력 → 엔터:
   ```
   http://localhost:5173/
   ```
3. 웹앱이 보이면 ✅ 성공!
   - 화면 위쪽에 "월간 조업도", "작업장별 배정 현황", "로직 관리", "로직 디자이너" 탭이 보입니다

⚠ 이 상태에서는 **이 PC에서만** 접속 가능합니다. 다른 사람도 보려면 다음 단계 6으로.

### 단계 5-3: 잠깐 — PowerShell 창은 닫지 마세요

이 PowerShell 창이 곧 "서버"입니다. 닫으면 웹앱이 꺼집니다.
나중에 끄려면 PowerShell 창에서 **Ctrl + C** 또는 창을 닫으면 됩니다.

---

## 6. 같은 네트워크의 다른 사람도 접속하게 만들기

같은 사무실·집·Wi-Fi 안의 다른 PC·스마트폰에서도 보게 하려면 다음을 합니다.

### 단계 6-1: 기존 PowerShell 끄기

1. 실행 중인 PowerShell 창에서 **Ctrl + C** 누름
2. 메시지가 멈춤

### 단계 6-2: 네트워크 노출 모드로 다시 실행

같은 창에 입력:

```
npm run dev -- --host
```

(`--` 두 개 + 띄어쓰기 + `--host` — 띄어쓰기 정확하게)

이번엔 다음과 비슷한 메시지:
```
  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.0.50:5173/
  ➜  Network: http://172.20.10.2:5173/
```

**Network 옆 주소**가 다른 사람이 접속할 주소입니다. 위 예에서는 `http://192.168.0.50:5173/`

### 단계 6-3: 다른 사람에게 알리기

같은 Wi-Fi에 연결된 사람에게 그 Network 주소를 전달:
- 카톡으로 보내기
- 종이에 적어 알려주기

다른 사람은 브라우저에 그 주소를 입력하면 웹앱이 보입니다.

### 단계 6-4: 접속이 안 될 때

다른 PC에서 접속 시도했는데 "접속할 수 없음" 같은 메시지가 나오면 → **단계 7 방화벽 허용**을 진행하세요.

---

## 7. 방화벽 허용 (필요할 때만)

Windows 방화벽이 외부 접속을 막고 있을 수 있습니다. 한 번만 허용해주면 됩니다.

### 단계 7-1: 관리자 PowerShell 열기

1. **Windows 키** 누르기
2. `powershell` 입력
3. 검색 결과 "Windows PowerShell"에 **오른쪽 마우스 클릭**
4. **"관리자 권한으로 실행"** 선택
5. UAC 창이 뜨면 **"예"**

(일반 PowerShell이 아닌 **관리자** 권한이어야 합니다 — 창 제목에 "관리자:" 라고 적혀 있어야 함)

### 단계 7-2: 방화벽 규칙 추가

관리자 PowerShell에 다음 입력 → 엔터:

```
New-NetFirewallRule -DisplayName "Vite Dev Server" -Direction Inbound -LocalPort 5173 -Protocol TCP -Action Allow
```

`Name`, `DisplayName` 같은 줄이 출력되면 OK.

### 단계 7-3: 다시 접속 시도

이제 다른 사람이 다시 Network 주소로 접속해보면 됩니다.

### 만약 그래도 안 된다면

회사·기관 네트워크는 추가 보안 정책이 있을 수 있어요. 시스템 관리자에게 5173 포트 인바운드 허용을 문의해야 합니다.

---

## 8. 종료 및 재시작

### 끄기

1. 실행 중인 PowerShell 창에서 **Ctrl + C** 누름
2. 또는 그냥 창 닫기

### 다음 번 켜기 (한번 설치 후엔 간단)

1. **Windows 키 + R** → `powershell` → 확인
2. PowerShell에서 폴더 이동:
   ```
   cd "C:\Users\본인이름\Downloads\block-assignment-system-main\web"
   ```
3. 실행:
   ```
   npm run dev -- --host
   ```
4. 표시된 Network 주소로 접속

→ 매번 약 30초 ~ 1분이면 됨.

---

## 9. 문제 해결 모음

| 증상 | 원인 | 해결 |
|---|---|---|
| `node` 명령이 인식 안 됨 | Node.js 설치 미완 또는 PATH 갱신 안 됨 | PC 재부팅 |
| `npm install` 중 빨간 ERROR | 네트워크/프록시 문제 | Wi-Fi 다시 연결 후 재시도 |
| `npm install` 중 SSL 에러 (사내망) | 회사 프록시 | 회사 IT에 npm 프록시 설정 문의 |
| `localhost:5173`만 열림, 다른 PC에서 안 됨 | `--host` 옵션 빠짐 | `npm run dev -- --host` 로 다시 |
| 다른 PC에서 시간 초과 | 방화벽 | 단계 7 진행 |
| 한글 화면이 깨짐 | 브라우저 호환 문제 | 크롬·엣지 최신 버전 사용 |
| 5173 포트가 안 된다 | 다른 프로그램이 사용 중 | Vite가 자동으로 5174 등으로 옮김 — 콘솔에 표시되는 실제 포트 확인 |
| PowerShell 창에서 한글 입력 안 됨 | IME 문제 | 별도 입력기 또는 영어로 입력 |
| Ctrl+C 눌렀더니 "Y/N" 물어봄 | 종료 확인 | `Y` 입력 후 엔터 |
| Github에서 ZIP을 못 받음 (404) | 저장소가 Private | 저장소 주인에게 Public 변경 요청 |

### 화면이 이상해 보일 때

데이터가 안 보이거나 일부 화면이 비어있다면:
1. 브라우저에서 **Ctrl + F5** (강제 새로고침)
2. 그래도 안 되면 PowerShell 종료 → 재시작 → 브라우저 새로고침

---

## 10. 용어 사전

| 용어 | 뜻 |
|---|---|
| **PowerShell** | Windows의 명령어 입력 창 (검은 창) |
| **명령어** | PowerShell에 타이핑하는 글자. 엔터를 누르면 실행됨 |
| **Node.js** | JavaScript를 컴퓨터에서 실행시키는 프로그램 |
| **npm** | Node.js와 함께 깔리는 도구. 라이브러리 설치·실행에 사용 |
| **의존성(dependency)** | 이 웹앱이 동작하려면 필요한 다른 작은 프로그램들 |
| **package.json** | 의존성 목록이 적힌 파일 (자동으로 사용됨) |
| **node_modules** | 실제 의존성 파일이 들어있는 폴더 (수백 MB) |
| **포트(port)** | 컴퓨터의 통신 창구 번호. 이 앱은 5173번 사용 |
| **localhost** | 본인 PC를 가리키는 주소 |
| **LAN / 네트워크** | 같은 Wi-Fi/회사망에 연결된 PC들의 집합 |
| **IP 주소** | 네트워크 안의 PC를 구분하는 번호 (예: 192.168.0.50) |
| **방화벽** | PC를 외부 접속으로부터 보호하는 보안 기능 |
| **Vite** | 이 웹앱을 띄워주는 개발 서버 도구 |

---

## 한눈에 보는 전체 흐름

```
[처음 한 번]
1. Node.js 설치 (~5분)
2. GitHub에서 ZIP 다운로드 + 압축 풀기 (~1분)
3. PowerShell 열고 web 폴더로 이동
4. npm install 실행 (~1~2분)
5. (필요시) 방화벽 5173 허용

[매번 켤 때]
6. PowerShell 열고 web 폴더로 이동
7. npm run dev -- --host
8. Network 주소를 다른 사람에게 공유
9. 끝낼 땐 Ctrl + C
```

이게 전부입니다. 문제가 생기면 **9. 문제 해결 모음**부터 확인하세요.

---

## 추가: 만약 이 PC가 진짜 "서버"처럼 항상 켜둘 거라면

Windows 시작 시 자동으로 실행되게 할 수도 있지만, 그건 더 복잡합니다. 일단은:
- PowerShell 창을 켜놓고 작업
- 끄지 않으면 계속 호스팅됨
- PC를 끄면 호스팅도 같이 멈춤

진짜 24시간 운영하려면 Vercel · Netlify · Cloudflare Pages 같은 외부 호스팅 서비스 이용을 권장합니다 (README 또는 CLAUDE.md 참고).

---

문서 작성일: 2026-05-27
대상 시스템: 작업장 조업도 / 블록 배정 시스템 (block-assignment-system)
저장소: https://github.com/domado01/block-assignment-system
