// 이전 버전(파라미터 편집형)의 브라우저 재배정 로직은 새 8단계 로직으로 교체되며
// 함께 제거됐다. 현재는 `generate_excel.py` 가 단독 source of truth 이며,
// 웹앱은 그 결과(data.json)를 그대로 표시만 한다.
//
// 향후 브라우저에서 로직을 즉시 재계산해야 한다면 이 파일에 8단계 로직을
// JS 로 미러링하면 된다 (참고: generate_excel.py 의 "최종작업장 배정 — 8단계 로직").
export {}
